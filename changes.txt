$ curl -s -X POST -H "session:{string}" -H "fire-options:{\"json_pretty_print\":1}" --data @new_quest.json https://localhost/api/v2/quests
    # where new_question.json contains the following:
    # {
    #    "query_text" : "Get System from all machines"
    # }
